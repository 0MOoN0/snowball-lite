FROM nginx:latest
# 安装node
# RUN curl -sL https://deb.nodesource.com/setup_18.x | bash -
# RUN apt-get install -y nodejs
# # RUN apt-get install -y nodejs
# # 将工作目录切换到/app目录下
# WORKDIR /app
# # 将当前目录的依赖描述文件复制到/app目录下
# COPY package.json package.json
# # 使用淘宝镜像安装依赖
# RUN npm install --registry=https://registry.npm.taobao.org
# # 将当前目录下的所有文件复制到/app目录下
# COPY . .
# RUN ls /app
#  # 添加一个ARG指令
# # ARG DUMMY
# # 执行npm run build:dev命令
# RUN npm run build:dev
# RUN ls /app
# COPY ./dist-dev/ /usr/share/nginx/html/snow_view
# COPY ./nginx_config/nginx.conf /etc/nginx/nginx.conf
COPY ./deploy/local_docker_compose/nginx_config/conf.d /etc/nginx/conf.d